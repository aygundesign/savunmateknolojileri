<!DOCTYPE html>
<html lang="tr">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>ISK Vision Technologies</title>

  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&display=swap" rel="stylesheet">

  <style>
    :root{
      --bg-main:#03040A;
      --bg-secondary:#071A2F;
      --panel:rgba(7,26,47,0.58);
      --panel-strong:rgba(9,25,45,0.82);
      --cyan:#00F5FF;
      --blue:#007BFF;
      --purple:#6C3BFF;
      --gold:#FFD34D;
      --white:#F5F7FA;
      --muted:#8A94A6;
      --line:rgba(255,255,255,0.08);
    }

    *{
      margin:0;
      padding:0;
      box-sizing:border-box;
      font-family:'Inter',sans-serif;
    }

    html{
      scroll-behavior:smooth;
    }

    body{
      min-height:100vh;
      background:var(--bg-main);
      color:var(--white);
      overflow-x:hidden;
    }

    body::before{
      content:"";
      position:fixed;
      inset:0;
      background:
        radial-gradient(circle at 20% 20%, rgba(0,245,255,0.12), transparent 28%),
        radial-gradient(circle at 78% 12%, rgba(108,59,255,0.14), transparent 26%),
        radial-gradient(circle at 65% 78%, rgba(255,211,77,0.07), transparent 26%),
        linear-gradient(180deg, #03040A 0%, #050916 48%, #03040A 100%);
      z-index:-4;
    }

    body::after{
      content:"";
      position:fixed;
      inset:0;
      background-image:
        linear-gradient(rgba(255,255,255,0.025) 1px, transparent 1px),
        linear-gradient(90deg, rgba(255,255,255,0.025) 1px, transparent 1px);
      background-size:80px 80px;
      mask-image:linear-gradient(to bottom, rgba(0,0,0,0.8), transparent 85%);
      z-index:-3;
      pointer-events:none;
    }

    #spaceCanvas{
      position:fixed;
      inset:0;
      z-index:-2;
      pointer-events:none;
    }

    .noise{
      position:fixed;
      inset:0;
      pointer-events:none;
      opacity:0.035;
      background-image:url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noiseFilter'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.8' numOctaves='3' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noiseFilter)'/%3E%3C/svg%3E");
      z-index:-1;
    }

    .container{
      width:min(1180px, calc(100% - 40px));
      margin:0 auto;
    }

    header{
      position:fixed;
      top:0;
      left:0;
      width:100%;
      z-index:100;
      background:linear-gradient(to bottom, rgba(3,4,10,0.88), rgba(3,4,10,0.42));
      backdrop-filter:blur(18px);
      border-bottom:1px solid rgba(255,255,255,0.06);
    }

    .nav{
      height:86px;
      display:flex;
      align-items:center;
      justify-content:space-between;
      gap:30px;
    }

    .brand{
      display:flex;
      align-items:center;
      gap:14px;
      text-decoration:none;
      color:var(--white);
    }

    .brand img{
      height:46px;
      width:auto;
      display:block;
      filter:drop-shadow(0 0 18px rgba(0,245,255,0.18));
    }

    .brand-mark{
      width:44px;
      height:44px;
      display:grid;
      place-items:center;
      border-radius:14px;
      background:linear-gradient(135deg, rgba(0,245,255,0.16), rgba(108,59,255,0.16));
      border:1px solid rgba(0,245,255,0.22);
      color:var(--cyan);
      font-weight:900;
      letter-spacing:-1px;
      box-shadow:0 0 30px rgba(0,245,255,0.10);
    }

    .brand-text strong{
      display:block;
      font-size:15px;
      letter-spacing:0.5px;
    }

    .brand-text span{
      display:block;
      font-size:11px;
      color:var(--muted);
      margin-top:3px;
      letter-spacing:1.2px;
      text-transform:uppercase;
    }

    nav{
      display:flex;
      align-items:center;
      gap:28px;
    }

    nav a{
      color:rgba(245,247,250,0.78);
      text-decoration:none;
      font-size:14px;
      font-weight:500;
      transition:0.25s ease;
      position:relative;
    }

    nav a::after{
      content:"";
      position:absolute;
      left:0;
      bottom:-9px;
      width:0;
      height:1px;
      background:linear-gradient(90deg,var(--cyan),var(--purple));
      transition:0.25s ease;
    }

    nav a:hover{
      color:var(--white);
    }

    nav a:hover::after{
      width:100%;
    }

    .nav-cta{
      padding:12px 18px;
      border-radius:999px;
      color:#041018;
      background:linear-gradient(90deg,var(--cyan),#74FBFF);
      text-decoration:none;
      font-size:13px;
      font-weight:800;
      box-shadow:0 0 26px rgba(0,245,255,0.18);
      transition:0.28s ease;
    }

    .nav-cta:hover{
      transform:translateY(-2px);
      box-shadow:0 0 40px rgba(0,245,255,0.28);
    }

    .hero{
      min-height:100vh;
      padding:150px 0 70px;
      display:flex;
      align-items:center;
    }

    .hero-grid{
      display:grid;
      grid-template-columns:1.08fr 0.92fr;
      gap:54px;
      align-items:center;
    }

    .eyebrow{
      display:inline-flex;
      align-items:center;
      gap:10px;
      padding:10px 14px;
      border:1px solid rgba(0,245,255,0.16);
      border-radius:999px;
      background:rgba(7,26,47,0.36);
      color:rgba(245,247,250,0.82);
      font-size:12px;
      letter-spacing:1.3px;
      text-transform:uppercase;
      margin-bottom:26px;
    }

    .eyebrow i{
      width:8px;
      height:8px;
      border-radius:999px;
      background:var(--cyan);
      box-shadow:0 0 18px var(--cyan);
    }

    h1{
      font-size:clamp(48px, 7vw, 92px);
      line-height:0.98;
      letter-spacing:-4px;
      font-weight:900;
      max-width:820px;
    }

    .gradient-text{
      background:linear-gradient(90deg,#fff 0%,#BFFBFF 34%,var(--cyan) 60%,#8C7BFF 100%);
      -webkit-background-clip:text;
      -webkit-text-fill-color:transparent;
    }

    .hero-text p{
      margin-top:26px;
      max-width:650px;
      color:rgba(245,247,250,0.72);
      font-size:18px;
      line-height:1.75;
    }

    .hero-actions{
      margin-top:34px;
      display:flex;
      gap:14px;
      flex-wrap:wrap;
    }

    .btn{
      padding:15px 22px;
      border-radius:999px;
      text-decoration:none;
      font-size:14px;
      font-weight:800;
      transition:0.28s ease;
      display:inline-flex;
      align-items:center;
      gap:10px;
    }

    .btn-primary{
      background:linear-gradient(90deg,var(--cyan),#8C7BFF);
      color:#031014;
      box-shadow:0 0 38px rgba(0,245,255,0.18);
    }

    .btn-secondary{
      color:var(--white);
      border:1px solid rgba(255,255,255,0.10);
      background:rgba(255,255,255,0.035);
      backdrop-filter:blur(12px);
    }

    .btn:hover{
      transform:translateY(-3px);
    }

    .hero-metrics{
      margin-top:44px;
      display:grid;
      grid-template-columns:repeat(3,1fr);
      gap:16px;
      max-width:620px;
    }

    .metric{
      padding:18px;
      border:1px solid rgba(255,255,255,0.08);
      border-radius:22px;
      background:rgba(7,26,47,0.34);
      backdrop-filter:blur(14px);
    }

    .metric strong{
      display:block;
      font-size:28px;
      color:var(--white);
      letter-spacing:-1px;
    }

    .metric span{
      display:block;
      margin-top:6px;
      font-size:12px;
      color:var(--muted);
    }

    .vision-card{
      position:relative;
      min-height:570px;
      border:1px solid rgba(255,255,255,0.10);
      background:
        linear-gradient(180deg, rgba(255,255,255,0.065), rgba(255,255,255,0.025)),
        rgba(7,26,47,0.44);
      border-radius:38px;
      overflow:hidden;
      backdrop-filter:blur(22px);
      box-shadow:
        0 40px 120px rgba(0,0,0,0.45),
        inset 0 1px 0 rgba(255,255,255,0.08);
    }

    .vision-card::before{
      content:"";
      position:absolute;
      inset:-30%;
      background:
        radial-gradient(circle at 50% 30%, rgba(0,245,255,0.18), transparent 25%),
        radial-gradient(circle at 70% 60%, rgba(108,59,255,0.16), transparent 24%),
        radial-gradient(circle at 35% 75%, rgba(255,211,77,0.10), transparent 20%);
      filter:blur(18px);
    }

    .vision-top{
      position:relative;
      z-index:2;
      display:flex;
      align-items:center;
      justify-content:space-between;
      padding:22px;
      border-bottom:1px solid rgba(255,255,255,0.08);
    }

    .window-dots{
      display:flex;
      gap:8px;
    }

    .window-dots span{
      width:10px;
      height:10px;
      border-radius:999px;
      background:rgba(255,255,255,0.18);
    }

    .status{
      display:flex;
      align-items:center;
      gap:8px;
      color:rgba(245,247,250,0.72);
      font-size:12px;
      letter-spacing:1px;
      text-transform:uppercase;
    }

    .status i{
      width:8px;
      height:8px;
      border-radius:999px;
      background:#00FF88;
      box-shadow:0 0 18px #00FF88;
    }

    .vision-screen{
      position:relative;
      z-index:2;
      padding:28px;
      min-height:490px;
    }

    .scanner{
      position:absolute;
      inset:28px;
      border-radius:28px;
      border:1px solid rgba(0,245,255,0.18);
      overflow:hidden;
      background:
        linear-gradient(rgba(0,245,255,0.05) 1px, transparent 1px),
        linear-gradient(90deg, rgba(0,245,255,0.05) 1px, transparent 1px);
      background-size:34px 34px;
    }

    .scanner::before{
      content:"";
      position:absolute;
      left:0;
      top:-30%;
      width:100%;
      height:28%;
      background:linear-gradient(to bottom, transparent, rgba(0,245,255,0.18), transparent);
      animation:scan 4s linear infinite;
    }

    @keyframes scan{
      from{top:-30%;}
      to{top:105%;}
    }

    .target{
      position:absolute;
      left:50%;
      top:50%;
      width:230px;
      height:230px;
      transform:translate(-50%,-50%);
      border-radius:34px;
      border:1px solid rgba(0,245,255,0.28);
      box-shadow:0 0 60px rgba(0,245,255,0.12);
    }

    .target::before,
    .target::after{
      content:"";
      position:absolute;
      inset:26px;
      border:1px solid rgba(255,255,255,0.12);
      border-radius:26px;
    }

    .target::after{
      inset:72px;
      border-color:rgba(255,211,77,0.22);
    }

    .drone-shape{
      position:absolute;
      left:50%;
      top:50%;
      width:92px;
      height:92px;
      transform:translate(-50%,-50%) rotate(45deg);
      border:2px solid rgba(0,245,255,0.8);
      border-radius:24px;
      background:rgba(0,245,255,0.06);
      box-shadow:0 0 34px rgba(0,245,255,0.30);
      animation:float 4.6s ease-in-out infinite;
    }

    .drone-shape::before,
    .drone-shape::after{
      content:"";
      position:absolute;
      background:rgba(255,255,255,0.50);
    }

    .drone-shape::before{
      width:150px;
      height:2px;
      left:-30px;
      top:45px;
    }

    .drone-shape::after{
      width:2px;
      height:150px;
      top:-30px;
      left:45px;
    }

    @keyframes float{
      0%,100%{transform:translate(-50%,-50%) rotate(45deg) translateY(0);}
      50%{transform:translate(-50%,-50%) rotate(45deg) translateY(-12px);}
    }

    .data-pill{
      position:absolute;
      z-index:3;
      padding:12px 14px;
      border-radius:16px;
      border:1px solid rgba(255,255,255,0.10);
      background:rgba(3,4,10,0.54);
      backdrop-filter:blur(14px);
      font-size:12px;
      color:rgba(245,247,250,0.78);
    }

    .data-pill strong{
      display:block;
      margin-top:4px;
      color:var(--cyan);
      font-size:15px;
    }

    .pill-1{left:38px;top:88px;}
    .pill-2{right:42px;top:120px;}
    .pill-3{left:48px;bottom:86px;}
    .pill-4{right:52px;bottom:76px;}

    section{
      padding:92px 0;
      position:relative;
    }

    .section-head{
      text-align:center;
      max-width:760px;
      margin:0 auto 56px;
    }

    .label{
      color:var(--cyan);
      text-transform:uppercase;
      letter-spacing:2px;
      font-size:12px;
      font-weight:800;
      margin-bottom:14px;
    }

    .section-head h2{
      font-size:clamp(34px,5vw,58px);
      line-height:1.05;
      letter-spacing:-2px;
      font-weight:900;
    }

    .section-head p{
      margin-top:20px;
      color:rgba(245,247,250,0.68);
      line-height:1.75;
      font-size:16px;
    }

    .tech-grid{
      display:grid;
      grid-template-columns:repeat(4,1fr);
      gap:18px;
    }

    .tech-card{
      padding:28px;
      min-height:250px;
      border-radius:28px;
      background:var(--panel);
      border:1px solid rgba(255,255,255,0.08);
      backdrop-filter:blur(18px);
      transition:0.28s ease;
      position:relative;
      overflow:hidden;
    }

    .tech-card::before{
      content:"";
      position:absolute;
      inset:auto -40% -42% auto;
      width:220px;
      height:220px;
      background:radial-gradient(circle, rgba(0,245,255,0.12), transparent 70%);
    }

    .tech-card:hover{
      transform:translateY(-8px);
      border-color:rgba(0,245,255,0.24);
      box-shadow:0 24px 60px rgba(0,0,0,0.28);
    }

    .icon{
      width:54px;
      height:54px;
      border-radius:18px;
      display:grid;
      place-items:center;
      background:linear-gradient(135deg, rgba(0,245,255,0.16), rgba(108,59,255,0.15));
      border:1px solid rgba(0,245,255,0.16);
      color:var(--cyan);
      font-weight:900;
      margin-bottom:24px;
    }

    .tech-card h3{
      font-size:20px;
      margin-bottom:12px;
    }

    .tech-card p{
      color:rgba(245,247,250,0.64);
      line-height:1.7;
      font-size:14px;
    }

    .showcase{
      display:grid;
      grid-template-columns:0.9fr 1.1fr;
      gap:26px;
      align-items:stretch;
    }

    .showcase-info{
      padding:36px;
      border-radius:32px;
      background:var(--panel-strong);
      border:1px solid rgba(255,255,255,0.08);
      backdrop-filter:blur(18px);
    }

    .showcase-info h2{
      font-size:42px;
      line-height:1.08;
      letter-spacing:-1.5px;
    }

    .showcase-info p{
      margin-top:18px;
      color:rgba(245,247,250,0.66);
      line-height:1.75;
    }

    .feature-list{
      margin-top:30px;
      display:grid;
      gap:14px;
    }

    .feature-item{
      display:flex;
      gap:12px;
      align-items:flex-start;
      padding:15px;
      border-radius:18px;
      background:rgba(255,255,255,0.035);
      border:1px solid rgba(255,255,255,0.06);
    }

    .feature-item i{
      width:20px;
      height:20px;
      border-radius:999px;
      background:var(--cyan);
      box-shadow:0 0 20px rgba(0,245,255,0.28);
      flex:0 0 auto;
      margin-top:2px;
    }

    .feature-item strong{
      display:block;
      font-size:15px;
      margin-bottom:4px;
    }

    .feature-item span{
      color:var(--muted);
      font-size:13px;
      line-height:1.55;
    }

    .dashboard{
      min-height:560px;
      border-radius:32px;
      background:
        linear-gradient(180deg, rgba(255,255,255,0.06), rgba(255,255,255,0.02)),
        rgba(7,26,47,0.54);
      border:1px solid rgba(255,255,255,0.08);
      backdrop-filter:blur(18px);
      overflow:hidden;
      position:relative;
      padding:26px;
    }

    .dashboard::before{
      content:"";
      position:absolute;
      inset:0;
      background:
        radial-gradient(circle at 45% 40%, rgba(0,245,255,0.16), transparent 24%),
        radial-gradient(circle at 70% 70%, rgba(108,59,255,0.14), transparent 26%);
    }

    .map-panel{
      position:relative;
      z-index:2;
      height:100%;
      min-height:500px;
      border-radius:24px;
      border:1px solid rgba(0,245,255,0.16);
      overflow:hidden;
      background:
        linear-gradient(rgba(255,255,255,0.035) 1px, transparent 1px),
        linear-gradient(90deg, rgba(255,255,255,0.035) 1px, transparent 1px),
        radial-gradient(circle at center, rgba(0,245,255,0.08), rgba(3,4,10,0.55));
      background-size:42px 42px, 42px 42px, auto;
    }

    .route{
      position:absolute;
      left:12%;
      top:18%;
      width:76%;
      height:62%;
      border:1px dashed rgba(0,245,255,0.28);
      border-radius:44% 56% 46% 54%;
      animation:routePulse 3.8s ease-in-out infinite;
    }

    @keyframes routePulse{
      0%,100%{opacity:0.45;transform:scale(1);}
      50%{opacity:0.85;transform:scale(1.018);}
    }

    .map-dot{
      position:absolute;
      width:12px;
      height:12px;
      border-radius:999px;
      background:var(--cyan);
      box-shadow:0 0 22px var(--cyan);
    }

    .map-dot:nth-child(2){left:22%;top:30%;}
    .map-dot:nth-child(3){left:62%;top:26%;background:var(--gold);box-shadow:0 0 22px var(--gold);}
    .map-dot:nth-child(4){left:72%;top:68%;}
    .map-dot:nth-child(5){left:34%;top:72%;background:var(--purple);box-shadow:0 0 22px var(--purple);}

    .floating-card{
      position:absolute;
      z-index:5;
      right:28px;
      bottom:28px;
      width:230px;
      padding:18px;
      border-radius:20px;
      background:rgba(3,4,10,0.58);
      border:1px solid rgba(255,255,255,0.10);
      backdrop-filter:blur(18px);
    }

    .floating-card small{
      color:var(--muted);
      text-transform:uppercase;
      letter-spacing:1px;
    }

    .floating-card strong{
      display:block;
      margin-top:8px;
      font-size:24px;
      color:var(--cyan);
    }

    .use-grid{
      display:grid;
      grid-template-columns:repeat(3,1fr);
      gap:18px;
    }

    .use-card{
      padding:28px;
      border-radius:26px;
      background:rgba(255,255,255,0.035);
      border:1px solid rgba(255,255,255,0.08);
      min-height:180px;
      transition:0.28s ease;
    }

    .use-card:hover{
      background:rgba(0,245,255,0.055);
      border-color:rgba(0,245,255,0.20);
      transform:translateY(-5px);
    }

    .use-card h3{
      font-size:20px;
      margin-bottom:12px;
    }

    .use-card p{
      color:rgba(245,247,250,0.62);
      line-height:1.65;
      font-size:14px;
    }

    .process{
      display:grid;
      grid-template-columns:repeat(4,1fr);
      gap:18px;
    }

    .step{
      position:relative;
      padding:28px;
      border-radius:26px;
      background:var(--panel);
      border:1px solid rgba(255,255,255,0.08);
      min-height:220px;
    }

    .step-number{
      font-size:44px;
      font-weight:900;
      color:rgba(0,245,255,0.16);
      margin-bottom:24px;
    }

    .step h3{
      font-size:19px;
      margin-bottom:10px;
    }

    .step p{
      color:rgba(245,247,250,0.62);
      font-size:14px;
      line-height:1.65;
    }

    .cta{
      padding:80px 40px;
      border-radius:40px;
      text-align:center;
      background:
        radial-gradient(circle at 50% 20%, rgba(0,245,255,0.15), transparent 36%),
        linear-gradient(135deg, rgba(7,26,47,0.92), rgba(10,13,35,0.88));
      border:1px solid rgba(255,255,255,0.10);
      position:relative;
      overflow:hidden;
    }

    .cta h2{
      font-size:clamp(36px,5vw,64px);
      line-height:1.05;
      letter-spacing:-2px;
      max-width:820px;
      margin:0 auto;
    }

    .cta p{
      margin:22px auto 34px;
      max-width:680px;
      color:rgba(245,247,250,0.68);
      line-height:1.75;
    }

    footer{
      padding:42px 0;
      color:var(--muted);
      border-top:1px solid rgba(255,255,255,0.07);
    }

    .footer-inner{
      display:flex;
      justify-content:space-between;
      gap:20px;
      align-items:center;
      flex-wrap:wrap;
      font-size:14px;
    }

    .reveal{
      opacity:0;
      transform:translateY(26px);
      transition:0.7s ease;
    }

    .reveal.active{
      opacity:1;
      transform:translateY(0);
    }

    @media(max-width:1020px){
      .hero-grid,
      .showcase{
        grid-template-columns:1fr;
      }

      .tech-grid,
      .process{
        grid-template-columns:repeat(2,1fr);
      }

      .vision-card{
        min-height:520px;
      }
    }

    @media(max-width:760px){
      nav{
        display:none;
      }

      .nav{
        height:76px;
      }

      .nav-cta{
        display:none;
      }

      .hero{
        padding-top:120px;
      }

      h1{
        letter-spacing:-2px;
      }

      .hero-metrics,
      .tech-grid,
      .use-grid,
      .process{
        grid-template-columns:1fr;
      }

      .vision-card{
        min-height:480px;
        border-radius:28px;
      }

      .data-pill{
        display:none;
      }

      .section{
        padding:70px 0;
      }

      .showcase-info h2{
        font-size:34px;
      }

      .dashboard{
        min-height:460px;
      }
    }
  </style>
</head>

<body>
  <canvas id="spaceCanvas"></canvas>
  <div class="noise"></div>

  <header>
    <div class="container nav">
      <a href="#" class="brand">
        <img src="yenilogo.png" alt="Logo" onerror="this.style.display='none'; this.nextElementSibling.style.display='grid';">
        <div class="brand-mark" style="display:none;">IV</div>
        <div class="brand-text">
          <strong>ISK Vision Technologies</strong>
          <span>AI • Vision • Autonomous Systems</span>
        </div>
      </a>

      <nav>
        <a href="#teknoloji">Teknoloji</a>
        <a href="#sistem">Sistem</a>
        <a href="#alanlar">Kullanım Alanları</a>
        <a href="#surec">Süreç</a>
      </nav>

      <a href="#iletisim" class="nav-cta">İletişime Geç</a>
    </div>
  </header>

  <main>
    <section class="hero">
      <div class="container hero-grid">
        <div class="hero-text">
          <div class="eyebrow"><i></i> Görüntü işleme ve otonom teknoloji arayüzü</div>

          <h1>
            Geleceğin <span class="gradient-text">görüntü işleme</span> teknolojileri
          </h1>

          <p>
            Drone teknolojileri, yapay zekâ destekli analiz ve otonom sistem altyapıları için
            modern, güvenilir ve yüksek performanslı dijital çözümler.
          </p>

          <div class="hero-actions">
            <a href="#teknoloji" class="btn btn-primary">Teknolojiyi İncele →</a>
            <a href="#sistem" class="btn btn-secondary">Sistem Yapısını Gör</a>
          </div>

          <div class="hero-metrics">
            <div class="metric">
              <strong>AI</strong>
              <span>Yapay zekâ destekli analiz</span>
            </div>
            <div class="metric">
              <strong>CV</strong>
              <span>Computer vision altyapısı</span>
            </div>
            <div class="metric">
              <strong>RT</strong>
              <span>Gerçek zamanlı veri yaklaşımı</span>
            </div>
          </div>
        </div>

        <div class="vision-card reveal">
          <div class="vision-top">
            <div class="window-dots">
              <span></span><span></span><span></span>
            </div>
            <div class="status"><i></i> Vision Core Active</div>
          </div>

          <div class="vision-screen">
            <div class="scanner">
              <div class="target"></div>
              <div class="drone-shape"></div>
            </div>

            <div class="data-pill pill-1">
              Detection Layer
              <strong>98.4%</strong>
            </div>

            <div class="data-pill pill-2">
              Image Stream
              <strong>Real-Time</strong>
            </div>

            <div class="data-pill pill-3">
              AI Decision
              <strong>Ready</strong>
            </div>

            <div class="data-pill pill-4">
              Vision Model
              <strong>Adaptive</strong>
            </div>
          </div>
        </div>
      </div>
    </section>

    <section id="teknoloji">
      <div class="container">
        <div class="section-head reveal">
          <div class="label">Teknoloji Katmanları</div>
          <h2>Görüntüden veriye, veriden karara uzanan akıllı yapı.</h2>
          <p>
            Modern görüntü işleme sistemleri; kamera verisini algılar, işler, anlamlandırır
            ve otonom karar süreçleri için kullanılabilir hale getirir.
          </p>
        </div>

        <div class="tech-grid">
          <div class="tech-card reveal">
            <div class="icon">01</div>
            <h3>Görüntü İşleme</h3>
            <p>Kamera ve sensör verilerini analiz eden, nesne, alan ve hareket tespiti yapabilen temel teknoloji katmanı.</p>
          </div>

          <div class="tech-card reveal">
            <div class="icon">02</div>
            <h3>Yapay Zekâ Analizi</h3>
            <p>Görüntüden anlam çıkaran, sınıflandırma ve karar destek süreçlerine katkı sağlayan yapay zekâ yaklaşımı.</p>
          </div>

          <div class="tech-card reveal">
            <div class="icon">03</div>
            <h3>Otonom Sistem Mantığı</h3>
            <p>Algılanan çevre verisini aksiyona dönüştüren, akıllı sistem mimarileri için planlanmış yapı.</p>
          </div>

          <div class="tech-card reveal">
            <div class="icon">04</div>
            <h3>Drone Teknolojileri</h3>
            <p>Hava araçları, saha gözlemi, haritalama ve izleme sistemleri için geliştirilebilir teknoloji altyapısı.</p>
          </div>
        </div>
      </div>
    </section>

    <section id="sistem">
      <div class="container showcase">
        <div class="showcase-info reveal">
          <div class="label">Sistem Vizyonu</div>
          <h2>Akıllı analiz için tasarlanmış premium teknoloji deneyimi.</h2>
          <p>
            Bu arayüz; görüntü işleme, yapay zekâ, drone teknolojileri ve otonom sistemleri temsil eden
            modern bir teknoloji markası için hazırlanmıştır.
          </p>

          <div class="feature-list">
            <div class="feature-item">
              <i></i>
              <div>
                <strong>Gerçek zamanlı düşünce yapısı</strong>
                <span>Verinin hızlı işlenmesi ve karar süreçlerine aktarılması hedeflenir.</span>
              </div>
            </div>

            <div class="feature-item">
              <i></i>
              <div>
                <strong>Yüksek doğruluk yaklaşımı</strong>
                <span>Görüntü içindeki kritik nesne ve hareketlerin doğru analiz edilmesine odaklanır.</span>
              </div>
            </div>

            <div class="feature-item">
              <i></i>
              <div>
                <strong>Ölçeklenebilir mimari</strong>
                <span>Farklı sektör ve saha ihtiyaçlarına göre genişleyebilir bir teknoloji dili sunar.</span>
              </div>
            </div>
          </div>
        </div>

        <div class="dashboard reveal">
          <div class="map-panel">
            <div class="route"></div>
            <div class="map-dot"></div>
            <div class="map-dot"></div>
            <div class="map-dot"></div>
            <div class="map-dot"></div>

            <div class="floating-card">
              <small>Vision Analysis</small>
              <strong>Processing</strong>
              <p style="margin-top:8px;color:rgba(245,247,250,0.62);font-size:13px;line-height:1.5;">
                Görsel veri analizi ve akıllı karar altyapısı.
              </p>
            </div>
          </div>
        </div>
      </div>
    </section>

    <section id="alanlar">
      <div class="container">
        <div class="section-head reveal">
          <div class="label">Kullanım Alanları</div>
          <h2>Farklı sektörler için geliştirilebilir teknoloji altyapısı.</h2>
          <p>
            Görüntü işleme ve yapay zekâ sistemleri; sahadan gelen görsel veriyi daha anlamlı,
            daha hızlı ve daha yönetilebilir hale getirir.
          </p>
        </div>

        <div class="use-grid">
          <div class="use-card reveal">
            <h3>Drone Sistemleri</h3>
            <p>Hava görüntüsü analizi, rota yaklaşımı, saha gözlemi ve akıllı izleme senaryoları.</p>
          </div>

          <div class="use-card reveal">
            <h3>Endüstriyel Denetim</h3>
            <p>Üretim, tesis, ekipman ve saha kontrollerinde görüntü tabanlı analiz yaklaşımı.</p>
          </div>

          <div class="use-card reveal">
            <h3>Güvenlik ve İzleme</h3>
            <p>Alan takibi, hareket algılama ve kritik bölge kontrolü için akıllı görsel sistemler.</p>
          </div>

          <div class="use-card reveal">
            <h3>Haritalama</h3>
            <p>Drone ve görsel veri kaynaklarından elde edilen görüntülerin analiz edilmesi.</p>
          </div>

          <div class="use-card reveal">
            <h3>Akıllı Şehirler</h3>
            <p>Trafik, kalabalık, saha yoğunluğu ve çevresel analiz süreçlerinde kullanılabilir yapı.</p>
          </div>

          <div class="use-card reveal">
            <h3>Tarım Teknolojileri</h3>
            <p>Alan izleme, bitki sağlığı, bölgesel analiz ve görsel veri değerlendirme süreçleri.</p>
          </div>
        </div>
      </div>
    </section>

    <section id="surec">
      <div class="container">
        <div class="section-head reveal">
          <div class="label">Çalışma Yaklaşımı</div>
          <h2>Fikirden teknoloji sunumuna net ve güçlü süreç.</h2>
        </div>

        <div class="process">
          <div class="step reveal">
            <div class="step-number">01</div>
            <h3>Analiz</h3>
            <p>Projenin hedefi, kullanım alanı ve teknoloji ihtiyacı netleştirilir.</p>
          </div>

          <div class="step reveal">
            <div class="step-number">02</div>
            <h3>Arayüz Tasarımı</h3>
            <p>Konuya uygun, modern ve premium dijital deneyim oluşturulur.</p>
          </div>

          <div class="step reveal">
            <div class="step-number">03</div>
            <h3>Teknoloji Dili</h3>
            <p>Görüntü işleme, yapay zekâ ve otonom sistem vurgusu güçlü şekilde yansıtılır.</p>
          </div>

          <div class="step reveal">
            <div class="step-number">04</div>
            <h3>Yayın</h3>
            <p>Web sitesi mobil uyumlu ve hızlı şekilde yayına hazır hale getirilir.</p>
          </div>
        </div>
      </div>
    </section>

    <section id="iletisim">
      <div class="container">
        <div class="cta reveal">
          <h2>Görüntü işleme teknolojileri için güçlü bir dijital vitrin tasarlayalım.</h2>
          <p>
            Yapay zekâ, drone teknolojileri ve otonom sistemler alanında çalışan bir marka için
            modern, güvenilir ve etkileyici bir web arayüzü.
          </p>
          <a href="mailto:info@example.com" class="btn btn-primary">Projeyi Başlat →</a>
        </div>
      </div>
    </section>
  </main>

  <footer>
    <div class="container footer-inner">
      <span>© 2026 ISK Vision Technologies. Tüm hakları saklıdır.</span>
      <span>AI • Computer Vision • Autonomous Systems</span>
    </div>
  </footer>

  <script>
    const canvas = document.getElementById("spaceCanvas");
    const ctx = canvas.getContext("2d");

    let width, height, particles;

    function resizeCanvas(){
      width = canvas.width = window.innerWidth;
      height = canvas.height = window.innerHeight;

      particles = Array.from({length: Math.min(90, Math.floor(width / 18))}, () => ({
        x: Math.random() * width,
        y: Math.random() * height,
        vx: (Math.random() - 0.5) * 0.22,
        vy: (Math.random() - 0.5) * 0.22,
        r: Math.random() * 1.4 + 0.4
      }));
    }

    function draw(){
      ctx.clearRect(0,0,width,height);

      for(const p of particles){
        p.x += p.vx;
        p.y += p.vy;

        if(p.x < 0 || p.x > width) p.vx *= -1;
        if(p.y < 0 || p.y > height) p.vy *= -1;

        ctx.beginPath();
        ctx.arc(p.x,p.y,p.r,0,Math.PI*2);
        ctx.fillStyle = "rgba(0,245,255,0.34)";
        ctx.fill();
      }

      for(let i=0;i<particles.length;i++){
        for(let j=i+1;j<particles.length;j++){
          const a = particles[i];
          const b = particles[j];
          const dx = a.x - b.x;
          const dy = a.y - b.y;
          const d = Math.sqrt(dx*dx + dy*dy);

          if(d < 130){
            ctx.beginPath();
            ctx.moveTo(a.x,a.y);
            ctx.lineTo(b.x,b.y);
            ctx.strokeStyle = `rgba(0,245,255,${(1 - d / 130) * 0.10})`;
            ctx.lineWidth = 1;
            ctx.stroke();
          }
        }
      }

      requestAnimationFrame(draw);
    }

    window.addEventListener("resize", resizeCanvas);
    resizeCanvas();
    draw();

    const reveals = document.querySelectorAll(".reveal");

    const observer = new IntersectionObserver((entries)=>{
      entries.forEach(entry=>{
        if(entry.isIntersecting){
          entry.target.classList.add("active");
        }
      });
    }, {threshold:0.16});

    reveals.forEach(el=>observer.observe(el));
  </script>
</body>
</html>
